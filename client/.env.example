# The client talks to the backend via the /api proxy configured in vite.config.js
# during development, so no API base URL env var is required by default.
# If you deploy the frontend separately from the backend, set this and
# update src/api/axiosInstance.js to use it as the baseURL instead of '/api'.
VITE_API_URL=http://localhost:5000/api
